void main(argc: int) {
    let demo := true;
    let progress := 55;

    @println("Hello, {}!", "world");
    @println("Argument count: {}", argc);
    @println("");
    @println("Progress: {}% ({})", progress, demo);
}
