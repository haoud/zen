int factorial(n: int) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

int println(message: str) {
    // TODO...
    return 0;
}

int main() {
    println("Hello, world!");
    return factorial(5);
}
