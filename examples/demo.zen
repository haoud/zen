bool main() {
    let holidays := false;  // It's not holidays
    let money := 2000;      // I have 2000 in my bank account

    let work := !holidays && money <= 500;
    let happy := !work || money > 1000;

    return happy;
}