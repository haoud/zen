struct data {
    metadata: struct metadata,
    value: bool,
}

struct metadata {
    alignment: int,
    size: int,
}

// A simple Zen program
int main() {
    let value : int = { 1 };
    let data : struct data = { {8, 32}, true };
    var copy : struct data = { {8, 32}, true };
    //let array : bool[3] = { data.value, copy.value, false };
    return 0;
}